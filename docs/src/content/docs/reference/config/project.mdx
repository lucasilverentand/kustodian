---
title: Project Configuration
description: Complete reference for kustodian.yaml project files
---

The project file (`kustodian.yaml`) marks the root of a Kustodian project and contains project-level metadata.

## Schema

```yaml
apiVersion: kustodian.io/v1
kind: Project
metadata:
  name: <project-name>
spec:
  template_sources: # optional, see "External template sources"
    - name: <source-name>
      github: <owner>/<repo>@<ref>
```

## Fields

### apiVersion

| | |
|-|-|
| **Required** | Yes |
| **Type** | String |
| **Value** | `kustodian.io/v1` |

API version for the resource.

### kind

| | |
|-|-|
| **Required** | Yes |
| **Type** | String |
| **Value** | `Project` |

Resource type identifier.

### metadata

| | |
|-|-|
| **Required** | Yes |
| **Type** | Object |

Project metadata.

#### metadata.name

| | |
|-|-|
| **Required** | Yes |
| **Type** | String |
| **Pattern** | `^[a-z0-9][a-z0-9-]*[a-z0-9]$` |

Unique identifier for the project. Used in logging and error messages.

## Example

### Minimal

```yaml
apiVersion: kustodian.io/v1
kind: Project
metadata:
  name: my-infrastructure
```

### Full Example

```yaml
apiVersion: kustodian.io/v1
kind: Project
metadata:
  name: production-infrastructure
```

## External template sources

`spec.template_sources` lets you consume templates that live in another Git
repository, an HTTP archive, or an OCI registry. Each entry is fetched into
the project's local cache (`.kustodian/templates-cache`) and merged with the
templates found under `templates/`.

A single source can host either:

- **One template** — `template.yaml` at the root of the source path.
- **Many templates** — each subdirectory contains its own `template.yaml`.

Template names must be unique across all sources and the local `templates/`
directory.

### GitHub source

```yaml
spec:
  template_sources:
    # Shorthand: owner/repo@<branch|tag|commit>[:path]
    - name: nginx
      github: lucasilverentand/kustodian-templates@v1.0.0:nginx

    # Object form
    - name: monitoring
      github:
        repo: lucasilverentand/monitoring-templates
        ref:
          tag: v2.3.4
        path: kube-prometheus-stack
      ttl: 1h
```

The shorthand parser infers the ref kind:

- 40-character hex strings → `commit`
- Semver-shaped strings (e.g. `v1.2.3`, `1.0`) → `tag`
- Anything else → `branch`

### Git source

```yaml
spec:
  template_sources:
    - name: shared
      git:
        url: https://git.example.com/templates.git
        ref:
          tag: v1.0.0
        path: production
```

Exactly one of `ref.branch`, `ref.tag`, or `ref.commit` must be set. Tags and
commits are immutable and cached forever; branches are mutable and refreshed
according to `ttl` (default 1h).

### Working with sources

```bash
kustodian sources list           # show configured sources
kustodian sources fetch          # populate the cache
kustodian sources fetch --force  # refresh, ignoring TTL
kustodian sources versions <name>
kustodian sources cache info
```

Commands that load templates (`apply`, `diff`, `validate`, `update`) fetch
configured sources automatically and report cache hits in their output.

## Location

The `kustodian.yaml` file must be at the project root:

```
my-project/
├── kustodian.yaml    # <-- Here
├── node-profiles/
├── templates/
└── clusters/
```

## Project Discovery

Kustodian searches upward from the current directory to find `kustodian.yaml`. This allows running commands from any subdirectory:

```bash
cd clusters/production/nodes
kustodian validate  # Finds project root automatically
```

## Validation

The project file is validated for:

- Valid `apiVersion` value
- `kind` equals `Project`
- Non-empty `metadata.name`
- Name follows Kubernetes naming conventions

## Related Resources

- [Projects Concept](/concepts/projects/) - Project overview
- [Project Structure](/getting-started/project-structure/) - Directory layout
