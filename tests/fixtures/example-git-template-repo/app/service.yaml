apiVersion: v1
kind: Service
metadata:
  name: example-git-web-app
  namespace: example-git-web-app
spec:
  selector:
    app.kubernetes.io/name: example-git-web-app
  ports:
    - name: http
      port: 80
      targetPort: http
