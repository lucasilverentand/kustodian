apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: ${namespace}
  labels:
    app: nginx
spec:
  type: ${service_type}
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: nginx
